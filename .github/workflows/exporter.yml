name: Export

on: push

jobs:
  export:
    runs-on: ubuntu-latest
    name: Export PsBuilder
    steps:
      - uses: actions/checkout@v2
      - uses: actions/upload-artifact@v2
        with:
          name: app
          path: /home/runner/work/psbuilder/psbuilder/PsBuilder.init.ps1
